{
  "name": "pklinks-platform",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev":   "npm run dev  --workspace frontend",
    "build": "npm run build --workspace frontend",
    "start": "npm run dev  --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
